{
  "name": "human-inc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.js",
    "deploy": "npm run build && wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
