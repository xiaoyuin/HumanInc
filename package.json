{
  "name": "human-inc",
  "version": "0.1.2",
  "gameRelease": {
    "name": "Contains traces of human",
    "note": "Carbon-based dependencies remain unresolved."
  },
  "private": true,
  "type": "module",
  "scripts": {
    "prestart": "node scripts/version.js",
    "start": "node server.js",
    "build": "node scripts/build.js",
    "deploy": "npm run build && wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
